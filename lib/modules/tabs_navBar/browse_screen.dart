import 'package:flutter/material.dart';

class BrowsScreen extends StatelessWidget {
  const BrowsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
